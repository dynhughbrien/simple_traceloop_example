# simple_traceloop_example
Using "raw" urllib3 HTTP calls with Traceloop. 

When calling any LLM with just urllib3 library, genai_ attributes will NOT be generated. 

This code was provided just to demonstrate that case. 
